api:
  path: /api/v${api.version}
  version: 1
spring:
  application:
    name: shop-app
  jackson:
    serialization.write_dates_as_timestamps: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:mysql://localhost:3306/shopapp?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
server:
  port: 2345



logging:
  level:
    root: info

jwt:
  expiration: 5000
  secret-key: 2423bodsagtstor5642ellsdasfu1fts1114asdfvzxb
#  second
#    30 ngay = 30 * 24 * 60 * 60
#    1 ngay = 24 * 60 * 60
#    1 gio = 60 * 60